[project]
name = "kdc"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "modern-robotics",
    "setuptools",
]

[tool.uv.sources]
modern-robotics = { git = "https://github.com/NxRLab/ModernRobotics.git", subdirectory = "packages/Python" }
